{
  "name": "auto-cr-rules",
  "version": "2.0.1",
  "description": "Built-in lint rules for auto-cr-cmd",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.json --force",
    "prepublishOnly": "pnpm run build",
    "publish": "pnpm run build && npm publish --access public"
  },
  "license": "MIT",
  "sideEffects": false,
  "dependencies": {
    "@swc/types": "^0.1.25"
  },
  "devDependencies": {
    "tslib": "^2.8.1",
    "typescript": "^5.9.2"
  }
}
